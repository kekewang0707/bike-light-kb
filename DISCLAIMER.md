# 免责声明 / 使用条款

> 使用本仓库代码前，请先读完这一页。

## 1. 项目定位

`bike-light-kb` 是一个**个人学习与技术研究**项目，用于探索「电商数据采集 → 结构化建模 → LLM 语义分析 → 选品决策」的完整链路。
它不是商品，不提供服务，不得用于任何商业用途。

## 2. 数据来源与合规边界

- 本项目采集的数据来自第三方电商平台页面。**这些数据不属于本项目，也不因采集行为而转移权利**。
- 淘宝/天猫等平台的服务协议通常**明确禁止**自动化抓取。本仓库保留了逆向 mtop 签名、TLS 指纹伪装等实现，
  仅用于学习反爬与反反爬的技术原理。**运行这些代码可能违反目标平台的服务条款**，风险由运行者自行承担。
- 建议优先使用**平台官方开放 API / 联盟 API**获取商品数据。若你拥有目标平台的书面授权，
  可通过 `BKL_CRAWLER_RESPECT_ROBOTS=false` 关闭 robots 限制。

### 已内置的合规护栏

| 护栏 | 实现 | 配置 |
|------|------|------|
| robots.txt 遵从 | `crawler/compliance.py::RobotsChecker`（RFC 9309，5xx 时暂停抓取） | `BKL_CRAWLER_RESPECT_ROBOTS`（默认 `true`） |
| 全局限速 | `crawler/compliance.py::RateLimiter`（令牌桶） | `BKL_CRAWLER_RATE_PER_MINUTE`（默认 20/分钟） |
| 商品间随机延迟 | `crawler/engine.py` 15~35 秒梯度 | 代码内固定 |
| 个人信息脱敏 | `crawler/pii.py`：昵称明文不落库，仅存 pepper 盐值 SHA-256 | `BKL_PII_PEPPER` |
| PII 保留期 | `scripts/anonymize_pii.py`：超期清空昵称哈希 | `BKL_DATA_RETENTION_DAYS`（默认 365 天） |

## 3. 个人信息（PIPL）

- 评价中的用户昵称属于个人信息。本项目遵循最小必要原则：**采集后立即哈希，明文不落库**。
- 评价正文、评分等仍可能包含用户自行披露的信息，请勿对外分发原始数据。
- 如需清空全部昵称哈希，执行 `scripts/anonymize_pii.py --days 1`（先加 `--dry-run` 预览行数）。
  注：`--days 0` 表示**关闭**自动清理，不会删除任何数据。

## 4. 数据使用限制

以下行为**明确禁止**：

- 转售、分发、公开发布采集到的原始数据或衍生数据集
- 将数据用于针对被采集平台的竞争性产品
- 用于骚扰用户、刷单、虚假评价等违反《电子商务法》《反不正当竞争法》的用途

## 5. 免责

本仓库按「现状」提供，**不含任何明示或暗示的担保**。作者不对以下情况负责：

- 因使用本代码导致的账号封禁、法律纠纷或经济损失
- 采集数据的准确性、完整性、时效性
- 基于本项目分析结果做出的任何商业决策

若你是平台方并认为本项目侵犯了你的权益，请通过 issue 联系，我们将配合停止相关行为。
