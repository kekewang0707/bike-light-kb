# 自行车灯电商知识库 — 分功能实施计划

> 基于现有项目骨架，按功能模块拆解，每个模块可独立开发、测试、交付。

---

## 项目当前状态

```
bike-light-kb/
├── app.py                  # Streamlit 入口（骨架已建，含DB健康检查+数据概览）
├── config/settings.py      # Pydantic Settings 配置（已建）
├── requirements.txt        # 依赖清单（已建）
├── docker-compose.yml      # PostgreSQL 16 + ChromaDB（已建）
├── db/init.sql             # products + hot_rankings 两张表（已建）
├── crawler/__init__.py      # 空
├── knowledge_base/__init__.py  # 空
└── ui/__init__.py           # 空
```

---

## 一、功能模块总览

```
┌─────────────────────────────────────────────────────────────┐
│                      应用层 (Streamlit)                       │
│  ┌──────────────────┐  ┌──────────────┐  ┌───────────────┐ │
│  │  选品决策看板      │  │ 内容创作助手   │  │  系统管理后台   │ │
│  │  ui/dashboard/    │  │ ui/creator/   │  │  ui/admin/     │ │
│  └────────┬─────────┘  └──────┬───────┘  └───────┬───────┘ │
├───────────┼──────────────────┼──────────────────┼──────────┤
│           │        知识库服务层 (knowledge_base/)   │          │
│  ┌────────┴─────────┐  ┌──────┴───────┐  ┌───────┴───────┐ │
│  │ 卖点/场景/痛点查询  │  │ 图片语义搜索   │  │  向量混合检索   │ │
│  └────────┬─────────┘  └──────┬───────┘  └───────┬───────┘ │
├───────────┼──────────────────┼──────────────────┼──────────┤
│           │           分析引擎层                          │    │
│  ┌────────┴─────────┐  ┌──────┴───────┐  ┌───────┴───────┐ │
│  │ 评价语义提取(LLM)  │  │ 图片OCR+描述  │  │  CLIP向量聚类  │ │
│  └────────┬─────────┘  └──────┬───────┘  └───────┬───────┘ │
├───────────┼──────────────────┼──────────────────┼──────────┤
│           │           数据层                          │       │
│  ┌────────┴─────────────────┴──────────────────┴───────┐   │
│  │         PostgreSQL (商品/评价/规格/价格历史)            │   │
│  │         ChromaDB  (卖点向量/场景向量/图片描述向量)       │   │
│  └────────────────────────┬────────────────────────────┘   │
├──────────────────────────┼─────────────────────────────────┤
│           │              采集层                          │   │
│  ┌────────┴────────────────────────────────────────────┐   │
│  │  爬虫引擎 (Playwright) → 搜索列表 → 详情页 → 评价 → 图片  │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

共 **8 个功能模块**，建议按依赖关系依次开发：

| 序号 | 模块 | 依赖 | 目录 |
|:---|:---|:---|:---|
| M1 | 数据模型层 | 无 | `models/` |
| M2 | 爬虫引擎 | M1 | `crawler/` |
| M3 | 评价分析引擎 | M1 | `analysis/` |
| M4 | 图片分析管道 | M1 | `analysis/` |
| M5 | 知识库服务层 | M1, M3, M4 | `knowledge_base/` |
| M6 | 选品决策看板 | M1, M5 | `ui/dashboard/` |
| M7 | 内容创作助手 | M1, M5 | `ui/creator/` |
| M8 | 定时调度与监控 | M2 | `scheduler/` |

---

## 二、M1 — 数据模型层

**目标**：用 SQLAlchemy ORM 定义所有数据表，提供统一的 CRUD 操作接口。

### 2.1 文件结构

```
models/
├── __init__.py          # 导出所有模型 + get_session()
├── base.py              # declarative_base, engine, sessionmaker
├── product.py           # Product, ProductImage, ProductSKU
├── review.py            # Review, ReviewImage
├── price_history.py     # PriceHistory
├── ranking.py           # HotRanking
├── analysis.py          # ReviewAnalysis, ImageAnalysis
└── crud.py              # 通用 CRUD 辅助函数
```

### 2.2 数据表设计

**商品相关 (product.py)**

| 表名 | 关键字段 | 说明 |
|:---|:---|:---|
| Product | id, platform, platform_id, name, brand, shop_name, category, product_url, main_image_url, price, original_price, sales_volume, comment_count, good_rate, specs(JSONB), crawled_at, created_at, updated_at | 商品主表。platform+platform_id 做唯一约束 |
| ProductImage | id, product_id(FK), image_type(main/detail/video_cover), image_url, local_path, sort_order | 商品图片 |
| ProductSKU | id, product_id(FK), sku_name, price, stock, sku_specs(JSONB) | SKU规格 |

**评价相关 (review.py)**

| 表名 | 关键字段 | 说明 |
|:---|:---|:---|
| Review | id, product_id(FK), platform_review_id, rating(1-5), content(TEXT), user_name, user_level, buy_date, review_date, likes, reply_content, crawled_at | 评价主表 |
| ReviewImage | id, review_id(FK), image_url, local_path | 评价晒图 |

**价格历史 (price_history.py)**

| 表名 | 关键字段 | 说明 |
|:---|:---|:---|
| PriceHistory | id, product_id(FK), price, snapshot_date | 每日价格快照 |

**热榜 (ranking.py)**

| 表名 | 关键字段 | 说明 |
|:---|:---|:---|
| HotRanking | id, product_id(FK), ranking, list_name, period_type, fetched_at | 热榜快照 |

**分析结果 (analysis.py)**

| 表名 | 关键字段 | 说明 |
|:---|:---|:---|
| ReviewAnalysis | id, review_id(FK,UNIQUE), selling_points(JSONB), use_scenes(JSONB), pain_points(JSONB), user_profile(JSONB), summary(TEXT), analyzed_at | 评价语义分析结果 |
| ImageAnalysis | id, image_id(FK), ocr_text(TEXT), description(TEXT), style_tags(JSONB), clip_vector_id, analyzed_at | 图片分析结果 |

### 2.3 关键设计决策

- **JSONB 存规格和SKU**：自行车灯的规格字段因品牌而异（有的标流明，有的标照射距离），JSONB 灵活适配
- **platform + platform_id 做唯一约束**：替代当前的 `jd_product_id UNIQUE`，为多平台扩展做好准备
- **所有时间字段用 UTC**，展示层转换时区

### 2.4 实施步骤

| 步骤 | 内容 |
|:---|:---|
| 1 | 创建 `models/base.py` — engine + session工厂 |
| 2 | 依次创建各 model 文件 |
| 3 | 创建 `models/__init__.py` — 导出所有模型 + `init_db()` 自动建表 |
| 4 | 创建 `models/crud.py` — 通用增删改查辅助 |
| 5 | 更新 `db/init.sql` — 与 ORM 模型保持一致 |
| 6 | 编写 `scripts/seed_test_data.py` — 插入 5-10 条假数据跑通全链路 |

---

## 三、M2 — 爬虫引擎

**目标**：可靠地从京东、淘宝采集自行车灯商品数据，覆盖搜索列表→详情→评价→图片全链路。

### 3.1 文件结构

```
crawler/
├── __init__.py
├── engine.py             # CrawlerEngine — 爬虫主调度器
├── base.py               # BaseSpider — 抽象基类
├── jd_spider.py          # JDSpider — 京东实现
├── taobao_spider.py      # TaobaoSpider — 淘宝实现（Phase 4 启用）
├── downloader.py         # ImageDownloader — 异步图片下载器
├── parser.py             # 通用解析工具（价格格式化、数字提取等）
├── anti_detect.py        # 反爬对抗（UA轮换、延迟策略、Cookie管理）
├── pipeline.py           # DataPipeline — 采集→清洗→入库流水线
└── retry.py              # 重试策略（指数退避、断点续爬）
```

### 3.2 核心类设计

**CrawlerEngine (engine.py)** — 爬虫主调度器，串联搜索→详情→评价→图片→入库全流程

**BaseSpider (base.py)** — 抽象基类，定义 `search()`, `get_detail()`, `get_reviews()`, `get_price_history()` 接口

**JDSpider (jd_spider.py)**：
- 搜索：Playwright 移动端搜索页，按销量排序，提取 data-sku/标题/价格/评价数/店铺/主图
- 详情：解析 HTML，提取规格参数（`#detail .parameter-brand` 区域的 `<li>` 标签）、SKU、详情图
- 评价：抓取 AJAX 评价接口 `club.jd.com/comment/...`，分页获取
- 反爬：2-5秒随机延迟、UA池轮换（10+真实UA）、Playwright stealth模式、每日采集上限200商品

**ImageDownloader (downloader.py)** — 异步图片下载器，信号量控制并发(默认5)，目录结构 `data/images/{product_id}/{type}_{n}.jpg`

**DataPipeline (pipeline.py)** — 清洗(价格格式化/去HTML)+去重(platform+platform_id)+校验(必填/价格合理性/评分范围)+入库

### 3.3 实施步骤

| 步骤 | 内容 | 产出 |
|:---|:---|:---|
| 1 | 实现 `base.py` + 数据结构定义 | 接口契约 |
| 2 | 实现 `anti_detect.py` | 反爬基础 |
| 3 | 实现 `parser.py` | 工具函数 |
| 4 | 实现 `jd_spider.py.search()` | Top100商品列表 |
| 5 | 实现 `jd_spider.py.get_detail()` | 规格参数提取 |
| 6 | 实现 `jd_spider.py.get_reviews()` | 前5页评价 |
| 7 | 实现 `downloader.py` | 图片落地本地 |
| 8 | 实现 `pipeline.py` | 数据进 PostgreSQL |
| 9 | 实现 `engine.py` | 一键运行全链路 |
| 10 | 实现 `retry.py` | 断点续爬 |

### 3.4 关键设计决策

- **异步优先**：搜索、详情、图片下载全部用 `asyncio` + `httpx`/`aiohttp`
- **移动端页面优先**：京东移动端搜索页比PC端更容易解析，反爬更轻
- **Playwright 仅用于需要 JS 渲染的页面**，其余用 httpx 减少资源消耗
- **图片与数据分离**：爬虫只负责下载图片+记录路径；图片分析在 M4 中独立处理

---

## 四、M3 — 评价分析引擎

**目标**：用 LLM 从评价原文中提取结构化知识（卖点、使用场景、痛点、用户画像），向量化存入 ChromaDB。

### 4.1 文件结构

```
analysis/
├── __init__.py
├── llm_client.py         # LLM 客户端封装（DeepSeek / 通义千问）
├── prompts/
│   ├── __init__.py
│   ├── review_extract.py  # 评价提取 Prompt 模板
│   └── spec_normalize.py  # 规格参数标准化 Prompt
├── review_analyzer.py    # ReviewAnalyzer — 批量评价分析器
├── spec_normalizer.py    # SpecNormalizer — 规格参数标准化
└── batch_runner.py       # BatchRunner — 分批调用 + 限速 + 重试
```

### 4.2 核心类设计

**LLMClient (llm_client.py)**：封装 DeepSeek/通义千问 API，支持 `chat()` 单次调用和 `chat_batch()` 批量并发

**ReviewAnalyzer (review_analyzer.py)**：
- Prompt 设计：提取卖点(selling_points)、使用场景(use_scenes)、痛点(pain_points)、用户画像(user_profile)
- 每条评价提取结果包含：aspect(特性名)、mention(用户原话引用)、sentiment/severity/confidence
- 支持 `analyze_single()` 和 `analyze_batch()`

```
单条评价分析结果示例:
  selling_points: [{"aspect":"亮度","mention":"晚上照得很远","sentiment":"positive"}]
  use_scenes: [{"scene":"夜间通勤","detail":"每天下班走夜路用","confidence":0.9}]
  pain_points: []
  user_profile: {"rider_type":"通勤","bike_type":"山地车","concern_priority":["性价比"]}
  summary: "用户对亮度和续航满意，主要用于夜间通勤，注重性价比。"
```

### 4.3 实施步骤

| 步骤 | 内容 |
|:---|:---|
| 1 | 调通 DeepSeek API → `llm_client.py` |
| 2 | 手写 5 条自行车灯评价，调优 Prompt |
| 3 | 实现 `review_analyzer.py` — 单条 + 批量分析 |
| 4 | 实现 `batch_runner.py` — 分批+限速+失败重试 |
| 5 | 跑通全量评价分析（约5000条评价） |
| 6 | 抽样 100 条人工核验，调整 Prompt |

---

## 五、M4 — 图片分析管道

**目标**：对商品图片进行 OCR 文字提取、多模态描述生成、CLIP 向量化。

### 5.1 文件结构

```
analysis/
├── image/
│   ├── __init__.py
│   ├── ocr.py              # OCRExtractor — PaddleOCR 文字提取
│   ├── multimodal.py       # MultimodalDescriber — 多模态图片描述
│   ├── clip_vectorizer.py  # CLIPVectorizer — CLIP 向量+聚类
│   └── pipeline.py         # ImageAnalysisPipeline — 图片分析流水线
```

### 5.2 核心类设计

**OCRExtractor (ocr.py)**：PaddleOCR 提取图片文字，返回 full_text + text_blocks(含bbox和confidence) + key_phrases

**MultimodalDescriber (multimodal.py)**：调用 Qwen-VL/DeepSeek-VL2，生成图片描述 JSON（外观/安装方式/展示场景/视觉风格/文字信息）

**CLIPVectorizer (clip_vectorizer.py)**：OpenAI CLIP (ViT-B/32) 生成512维向量，支持向量化、K-Means风格聚类、以图搜图

**ImageAnalysisPipeline (pipeline.py)**：串联 OCR → 多模态描述 → CLIP向量化 → 结果入库

### 5.3 实施步骤

| 步骤 | 内容 |
|:---|:---|
| 1 | 安装 PaddleOCR，测试 10 张商品图 |
| 2 | 实现 `ocr.py` — 批量 OCR |
| 3 | 调通 Qwen-VL API，实现 `multimodal.py` |
| 4 | 实现 `clip_vectorizer.py` — 向量化+聚类+相似搜索 |
| 5 | 实现 `pipeline.py` — 端到端图片分析流水线 |
| 6 | 跑通全量图片分析，生成风格聚类报告 |

---

## 六、M5 — 知识库服务层

**目标**：封装 ChromaDB 操作，提供统一的语义查询接口。上层应用不直接操作向量数据库。

### 6.1 文件结构

```
knowledge_base/
├── __init__.py             # 导出 KnowledgeBase 门面类
├── chroma_client.py        # ChromaDB 客户端管理
├── vector_store.py         # 向量写入服务
├── query_service.py        # 语义查询服务（向量检索、混合检索）
├── collection_schemas.py   # 各 Collection 的 schema 定义
└── embedding.py            # Embedding 函数封装
```

### 6.2 ChromaDB Collection 设计

| Collection 名称 | 用途 | 维度 | 元数据字段 |
|:---|:---|:---|:---|
| `selling_points` | 卖点描述向量 | 1024 | product_id, review_id, aspect, mention_count |
| `use_scenes` | 使用场景向量 | 1024 | product_id, review_id, scene, confidence |
| `pain_points` | 痛点描述向量 | 1024 | product_id, review_id, aspect, severity |
| `product_descriptions` | 商品综合描述向量 | 1024 | product_id, name, brand, price_range |
| `image_descriptions` | 图片语义描述向量 | 1024 | product_id, image_id, image_type, style_tags |
| `image_clip` | CLIP 图片向量 | 512 | product_id, image_id, local_path |

### 6.3 KnowledgeBase 门面类

对外暴露的统一接口：
- **写入**：`index_reviews()`, `index_products()`, `index_images()`
- **查询**：`search_selling_points()`, `search_scenes()`, `search_pain_points()`, `hybrid_search()`, `search_images()`, `search_similar_images()`
- **聚合**：`get_category_insights()`, `get_product_knowledge()`

### 6.4 实施步骤

| 步骤 | 内容 |
|:---|:---|
| 1 | `chroma_client.py` — 连接管理 + Collection 初始化 |
| 2 | `embedding.py` — 封装 embedding 函数（text-embedding-3-small / bge-large-zh） |
| 3 | `collection_schemas.py` — 6 个 Collection 创建脚本 |
| 4 | `vector_store.py` — 向量批量写入 |
| 5 | `query_service.py` — 语义搜索 + 跨Collection检索 + 聚合 |
| 6 | `__init__.py` — KnowledgeBase 门面类组装 |

---

## 七、M6 — 选品决策看板

**目标**：运营人员每天打开即看品类全局、价格趋势、市场机会。

### 7.1 文件结构

```
ui/
├── __init__.py
├── common.py               # 公共组件（指标卡片、图表主题、DB连接装饰器）
├── dashboard/
│   ├── __init__.py
│   ├── overview.py         # 品类总览
│   ├── trends.py           # 趋势分析
│   ├── opportunity.py      # 机会洞察
│   └── product_detail.py   # 单品详情页
└── home.py                 # 主页（app.py 重构到此）
```

### 7.2 页面详情

**品类总览 (overview.py)**：
- 概览卡片：商品总数/均价/月销量/有图商品数/已分析评价数
- 图表：价格带分布柱状图、热销Top20表格、品牌分布饼图、评分分布直方图

**趋势分析 (trends.py)**：
- 销量趋势折线图（支持按价格段筛选）
- 价格趋势多条折线（均价/中位数/最低价/最高价）
- 新晋爆品榜单（近7天上榜 + 增速 > 30%）

**机会洞察 (opportunity.py)**：
- 价格带空白分析表（价格段 × 商品数 × 月销量 × 竞争度）
- 差评集中点词云 + 分布
- 品牌集中度（CR4 / HHI指数）
- AI 选品建议（调用 LLM 基于数据生成）

**单品详情 (product_detail.py)**：
- 基本信息 + 主图、价格历史走势、评价关键词云
- 好评高频卖点 / 差评槽点
- 用户使用场景分布 / 用户画像
- 同类竞品对比（价格相近 + 销量Top10）

### 7.3 实施步骤

| 步骤 | 内容 |
|:---|:---|
| 1 | 创建 `ui/common.py` — 统一样式、指标卡片组件、图表主题 |
| 2 | `overview.py` — 品类总览页面 |
| 3 | `trends.py` — 趋势分析页面 |
| 4 | `opportunity.py` — 机会洞察页面 |
| 5 | `product_detail.py` — 单品详情页 |
| 6 | 重构 `app.py` → `ui/home.py` — 多页面导航 |
| 7 | 全链路数据联调 |

---

## 八、M7 — 内容创作助手

**目标**：文案同学输入商品/品类，自动输出卖点洞察、竞品参考、AI 生成的标题和详情页框架。

### 8.1 文件结构

```
ui/
├── creator/
│   ├── __init__.py
│   ├── selling_point_insight.py  # 卖点洞察
│   ├── scene_and_avoid.py        # 场景+避坑指南
│   ├── ai_copywriter.py          # AI 文案生成
│   └── competitor_analysis.py    # 竞品文案分析
```

### 8.2 页面详情

**卖点洞察 (selling_point_insight.py)**：高频卖点词云 + 详情表（卖点×提及次数×好评率×典型评价原文）+ 竞品标题列表 + 标题关键词组合建议

**场景+避坑 (scene_and_avoid.py)**：真实使用场景展示（从评价提取，带原文引用）+ 常见槽点 & 文案回应建议

**AI 文案生成 (ai_copywriter.py)**：
- 输入：目标人群/价位/产品类型/风格偏好
- 输出：5个推荐标题 + 详情页框架（核心卖点/使用场景/规格参数/对比优势）+ 主图文案建议
- 核心 Prompt 设计原则：基于真实评价卖点、用数据说话、场景化表达、SEO标题规范、突出差异化

**竞品文案分析 (competitor_analysis.py)**：卖点覆盖矩阵 + 标题结构分析 + 差异化文案建议

### 8.3 实施步骤

| 步骤 | 内容 |
|:---|:---|
| 1 | `selling_point_insight.py` — 卖点洞察页 |
| 2 | `scene_and_avoid.py` — 场景+避坑页 |
| 3 | `competitor_analysis.py` — 竞品分析页 |
| 4 | `ai_copywriter.py` — AI 文案生成 |
| 5 | 整合 4 个页面到 content creator 子应用 |

---

## 九、M8 — 定时调度与监控

**目标**：爬虫定时执行、异常告警、数据新鲜度监控。

### 9.1 文件结构

```
scheduler/
├── __init__.py
├── jobs.py                # 任务定义
├── scheduler_app.py       # APScheduler 配置 + 启动
├── monitor.py             # 数据新鲜度监控
└── notifier.py            # 通知器（钉钉/企微 webhook）
```

### 9.2 核心任务

| 任务 | 时间 | 说明 |
|:---|:---|:---|
| crawl_daily | 凌晨2点 | 每日爬虫任务，采集最新商品数据和评价 |
| update_price_history | 凌晨3点 | 对所有活跃商品记录当日价格快照 |
| check_data_freshness | 早8点 | 检查最后爬虫时间、各表记录数是否正常 |
| generate_daily_report | 早9点 | LLM 生成日报摘要（新上榜/价格异动/高频关键词） |

### 9.3 监控项

- 爬虫健康：最近24h新数据量、错误率、图片下载成功率
- 数据库大小：各表记录数、增长速度
- 价格异常：单日涨跌>30%、价格倒挂
- 分析覆盖率：评价分析覆盖率、图片分析覆盖率

### 9.4 实施步骤

| 步骤 | 内容 |
|:---|:---|
| 1 | `jobs.py` — 定义定时任务 |
| 2 | `scheduler_app.py` — APScheduler 配置 |
| 3 | `monitor.py` — 数据监控检查 |
| 4 | `notifier.py` — 钉钉/企微通知 |
| 5 | 集成到 `app.py` 或独立进程 |

---

## 十、最终项目结构（Phase 3 结束时）

```
bike-light-kb/
├── app.py                      # Streamlit 入口（多页面）
├── docker-compose.yml
├── requirements.txt
├── .env                        # API keys + 敏感配置
├── Makefile                    # 常用命令快捷方式
│
├── config/
│   ├── settings.py             # Pydantic Settings
│   └── logging_config.py       # Loguru 配置
│
├── models/                     # M1: 数据模型层
│   ├── __init__.py
│   ├── base.py
│   ├── product.py
│   ├── review.py
│   ├── price_history.py
│   ├── ranking.py
│   ├── analysis.py
│   └── crud.py
│
├── crawler/                    # M2: 爬虫引擎
│   ├── __init__.py
│   ├── engine.py
│   ├── base.py
│   ├── jd_spider.py
│   ├── taobao_spider.py
│   ├── downloader.py
│   ├── parser.py
│   ├── anti_detect.py
│   ├── pipeline.py
│   └── retry.py
│
├── analysis/                   # M3 + M4: 分析引擎
│   ├── __init__.py
│   ├── llm_client.py
│   ├── review_analyzer.py
│   ├── spec_normalizer.py
│   ├── batch_runner.py
│   ├── prompts/
│   │   ├── __init__.py
│   │   ├── review_extract.py
│   │   └── spec_normalize.py
│   └── image/
│       ├── __init__.py
│       ├── ocr.py
│       ├── multimodal.py
│       ├── clip_vectorizer.py
│       └── pipeline.py
│
├── knowledge_base/             # M5: 知识库服务层
│   ├── __init__.py
│   ├── chroma_client.py
│   ├── vector_store.py
│   ├── query_service.py
│   ├── collection_schemas.py
│   └── embedding.py
│
├── ui/                         # M6 + M7: 应用层
│   ├── __init__.py
│   ├── home.py
│   ├── common.py
│   ├── dashboard/
│   │   ├── __init__.py
│   │   ├── overview.py
│   │   ├── trends.py
│   │   ├── opportunity.py
│   │   └── product_detail.py
│   └── creator/
│       ├── __init__.py
│       ├── selling_point_insight.py
│       ├── scene_and_avoid.py
│       ├── ai_copywriter.py
│       └── competitor_analysis.py
│
├── scheduler/                  # M8: 定时调度
│   ├── __init__.py
│   ├── jobs.py
│   ├── scheduler_app.py
│   ├── monitor.py
│   └── notifier.py
│
├── db/
│   ├── init.sql
│   └── migrations/
│
├── scripts/                    # 运维脚本
│   ├── seed_test_data.py
│   ├── run_crawler.py
│   ├── run_analysis.py
│   └── export_report.py
│
└── data/                       # 本地数据目录(.gitignore)
    ├── images/
    │   └── {product_id}/
    └── logs/
```

---

## 十一、开发顺序与里程碑

```
M1 (数据模型)
 │
 ├──► M2 (爬虫引擎) ───────────────┐
 │                                  │
 ├──► M3 (评价分析) ──► M5 (知识库) ─┼──► M6 (选品看板)
 │                                  │
 └──► M4 (图片分析) ────────────────┘     M7 (内容助手)
                                           │
                                      M8 (定时调度)
```

| 周次 | 完成的模块 | 可演示内容 |
|:---|:---|:---|
| 第1周 | M1 + M2 基础（列表+详情爬虫） | 数据库中有 50+ 商品数据 |
| 第2周 | M2 完整（评价+图片+Pipeline） | 100 商品 + 评价 + 图片全入库 |
| 第3周 | M3（评价分析） + M5（知识库写入） | 评价语义可搜索 |
| 第4周 | M4（图片分析） + M5（查询服务） | 图片可被语义检索 |
| 第5周 | M6（选品看板） | 运营可用看板看数据 |
| 第6周 | M7（内容助手） | 文案可用助手生成标题 |
| 第7-8周 | M8（调度监控） + 全链路优化 | 全自动运行 |

---

## 十二、风险与应对

| 模块 | 风险 | 应对 |
|:---|:---|:---|
| M2 爬虫 | 京东反爬导致爬虫失效 | 移动端页面优先；Playwright stealth；降低频率；备选：第三方数据API |
| M2 爬虫 | 详情页规格参数提取率低 | 允许 JSONB 存不完整数据；后期人工标注补全 |
| M3 评价分析 | LLM 返回格式不稳定 | response_format: json_object + Pydantic 校验 + 重试 |
| M3 评价分析 | LLM API 成本超预期 | 先跑100条测成本；用 DeepSeek（成本是 GPT-4 的 1/10） |
| M4 图片分析 | Qwen-VL 调用慢/贵 | 仅对主图做多模态描述；详情图只做 OCR |
| M4 图片分析 | CLIP 模型本地运行吃资源 | 用 ViT-B/32（最小模型）；<1000张图片时CPU可接受 |
| M5 知识库 | ChromaDB 单机性能瓶颈 | 初期 <10000 向量，ChromaDB 完全够用；Phase 4 再考虑 Milvus |
| M6/M7 UI | Streamlit 大数据量卡顿 | 数据查询加 @st.cache_data；图表数据预聚合 |
| M8 调度 | 爬虫任务超时 | 设置 30min 超时；下次执行自动恢复 |

---

## 十三、即刻创建文件结构

```bash
# 1. 模型层
mkdir -p models
touch models/{__init__,base,product,review,price_history,ranking,analysis,crud}.py

# 2. 爬虫模块
touch crawler/{engine,base,jd_spider,downloader,parser,anti_detect,pipeline,retry}.py

# 3. 分析引擎
mkdir -p analysis/prompts analysis/image
touch analysis/{__init__,llm_client,review_analyzer,spec_normalizer,batch_runner}.py
touch analysis/prompts/{__init__,review_extract,spec_normalize}.py
touch analysis/image/{__init__,ocr,multimodal,clip_vectorizer,pipeline}.py

# 4. 知识库
touch knowledge_base/{chroma_client,vector_store,query_service,collection_schemas,embedding}.py

# 5. UI 层
mkdir -p ui/dashboard ui/creator
touch ui/{home,common}.py
touch ui/dashboard/{__init__,overview,trends,opportunity,product_detail}.py
touch ui/creator/{__init__,selling_point_insight,scene_and_avoid,ai_copywriter,competitor_analysis}.py

# 6. 调度器
mkdir -p scheduler
touch scheduler/{__init__,jobs,scheduler_app,monitor,notifier}.py

# 7. 脚本
mkdir -p scripts
touch scripts/{seed_test_data,run_crawler,run_analysis,export_report}.py
```

### 启动开发的最小路径

1. **`models/base.py` + `models/product.py`** — 搞定数据库连接，能跑通第一条 insert
2. **`crawler/base.py`** — 定义爬虫接口，写死 5 条假数据让 M1 能跑通
3. **`crawler/jd_spider.py` 的 `search()`** — 跑通京东搜索列表抓取，拿到真数据
