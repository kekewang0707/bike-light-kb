# M1 — 数据模型层

**目标**：用 SQLAlchemy ORM 定义所有数据表，提供统一的 CRUD 操作接口。所有上层模块通过此层访问数据库。

**依赖**：无

**目录**：`models/`

---

## 1. 文件结构

```
models/
├── __init__.py          # 导出所有模型 + get_session() + init_db()
├── base.py              # declarative_base, engine, sessionmaker
├── product.py           # Product, ProductImage, ProductSKU
├── review.py            # Review, ReviewImage
├── price_history.py     # PriceHistory
├── ranking.py           # HotRanking
├── analysis.py          # ReviewAnalysis, ImageAnalysis
└── crud.py              # 通用 CRUD 辅助函数
```

---

## 2. 数据表设计

### 2.1 商品相关 (product.py)

**Product — 商品主表**

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | BIGSERIAL PK | |
| platform | VARCHAR(20) | taobao / pdd |
| platform_id | VARCHAR(100) | 平台商品ID |
| name | VARCHAR(500) | 商品标题 |
| brand | VARCHAR(200) | 品牌 |
| shop_name | VARCHAR(300) | 店铺名 |
| category | VARCHAR(200) | 品类 |
| product_url | VARCHAR(1000) | 商品链接 |
| main_image_url | VARCHAR(1000) | 主图URL |
| price | DECIMAL(10,2) | 当前售价 |
| original_price | DECIMAL(10,2) | 原价/划线价 |
| sales_volume | INTEGER | 销量 |
| comment_count | INTEGER | 评价数 |
| good_rate | DECIMAL(5,2) | 好评率(%) (如97.00) |
| specs | JSONB | 规格参数 {"流明":800,"防水":"IPX6","电池":"2000mAh"} |
| crawled_at | TIMESTAMP | 最后采集时间 |
| created_at | TIMESTAMP | |
| updated_at | TIMESTAMP | |

唯一约束：`(platform, platform_id)`

**ProductImage — 商品图片**

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | BIGSERIAL PK | |
| product_id | FK → Product | |
| image_type | VARCHAR(20) | main / detail / video_cover |
| image_url | VARCHAR(1000) | 原始URL |
| local_path | VARCHAR(500) | 本地存储路径 |
| sort_order | INTEGER | 排序 |

**ProductSKU — SKU规格**

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | BIGSERIAL PK | |
| product_id | FK → Product | |
| sku_name | VARCHAR(300) | "黑色-800流明-续航8小时" |
| price | DECIMAL(10,2) | SKU价格 |
| stock | INTEGER | 库存 |
| sku_specs | JSONB | {"颜色":"黑色","流明":800} |

### 2.2 评价相关 (review.py)

**Review — 评价主表**

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | BIGSERIAL PK | |
| product_id | FK → Product | |
| platform_review_id | VARCHAR(100) | 平台评价ID |
| rating | INTEGER | 1-5星 |
| content | TEXT | 评价原文 |
| user_name | VARCHAR(200) | |
| user_level | VARCHAR(50) | plus会员/普通用户 |
| buy_date | TIMESTAMP | 购买日期 |
| review_date | TIMESTAMP | 评价日期 |
| likes | INTEGER | 点赞数 |
| reply_content | TEXT | 卖家回复 |
| crawled_at | TIMESTAMP | |

**ReviewImage — 评价晒图**

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | BIGSERIAL PK | |
| review_id | FK → Review | |
| image_url | VARCHAR(1000) | |
| local_path | VARCHAR(500) | |

### 2.3 价格历史 (price_history.py)

**PriceHistory — 价格变动记录**

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | BIGSERIAL PK | |
| product_id | FK → Product | |
| price | DECIMAL(10,2) | |
| snapshot_date | DATE | |

### 2.4 热榜 (ranking.py)

**HotRanking — 热榜快照**

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | BIGSERIAL PK | |
| product_id | FK → Product | |
| ranking | INTEGER | 排名 |
| list_name | VARCHAR(200) | "自行车灯热销榜" |
| period_type | VARCHAR(50) | daily / weekly |
| fetched_at | TIMESTAMP | |

### 2.5 分析结果 (analysis.py)

**ReviewAnalysis — 评价语义分析结果**

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | BIGSERIAL PK | |
| review_id | FK → Review (UNIQUE) | 一对一 |
| selling_points | JSONB | `[{"aspect":"亮度高","mention":"...","sentiment":"positive"}]` |
| use_scenes | JSONB | `[{"scene":"夜骑山路","detail":"...","confidence":0.9}]` |
| pain_points | JSONB | `[{"aspect":"续航短","mention":"...","severity":"high"}]` |
| user_profile | JSONB | `{"rider_type":"通勤","bike_type":"山地车","concern_priority":["性价比"]}` |
| summary | TEXT | LLM 生成的评价摘要 |
| analyzed_at | TIMESTAMP | |

**ImageAnalysis — 图片分析结果**

| 字段 | 类型 | 说明 |
|:---|:---|:---|
| id | BIGSERIAL PK | |
| image_id | FK → ProductImage | |
| ocr_text | TEXT | OCR 提取的文字 |
| description | TEXT | 多模态模型生成的图片描述 |
| style_tags | JSONB | `["极简","运动风","科技感"]` |
| clip_vector_id | VARCHAR(100) | ChromaDB 中 CLIP 向量的 ID |
| analyzed_at | TIMESTAMP | |

---

## 3. 关系总览

```
Product 1──N ProductImage
Product 1──N ProductSKU
Product 1──N Review
Product 1──N PriceHistory
Product 1──N HotRanking

Review  1──N ReviewImage
Review  1──1 ReviewAnalysis

ProductImage 1──1 ImageAnalysis
```

---

## 4. 核心代码骨架

### base.py

```python
from sqlalchemy import create_engine
from sqlalchemy.orm import sessionmaker, declarative_base
from config.settings import settings

DATABASE_URL = (
    f"postgresql://{settings.db_user}:{settings.db_password}"
    f"@{settings.db_host}:{settings.db_port}/{settings.db_name}"
)

engine = create_engine(DATABASE_URL, pool_size=10, max_overflow=20)
SessionLocal = sessionmaker(bind=engine, autocommit=False, autoflush=False)
Base = declarative_base()

def get_session():
    """获取数据库会话（用于依赖注入）"""
    session = SessionLocal()
    try:
        yield session
    finally:
        session.close()

def init_db():
    """创建所有表"""
    Base.metadata.create_all(bind=engine)
```

### crud.py

```python
from typing import Type, TypeVar, Generic, List, Optional
from sqlalchemy.orm import Session

T = TypeVar("T")

class CRUDBase(Generic[T]):
    """通用 CRUD 基类"""

    def __init__(self, model: Type[T]):
        self.model = model

    def create(self, session: Session, **kwargs) -> T:
        obj = self.model(**kwargs)
        session.add(obj)
        session.commit()
        session.refresh(obj)
        return obj

    def get(self, session: Session, id: int) -> Optional[T]:
        return session.get(self.model, id)

    def get_or_create(self, session: Session, defaults: dict, **filters) -> T:
        obj = session.query(self.model).filter_by(**filters).first()
        if obj:
            return obj
        return self.create(session, **{**filters, **defaults})

    def list(self, session: Session, skip: int = 0, limit: int = 100) -> List[T]:
        return session.query(self.model).offset(skip).limit(limit).all()

    def update(self, session: Session, id: int, **kwargs) -> Optional[T]:
        obj = self.get(session, id)
        if obj:
            for key, value in kwargs.items():
                setattr(obj, key, value)
            session.commit()
            session.refresh(obj)
        return obj

    def delete(self, session: Session, id: int) -> bool:
        obj = self.get(session, id)
        if obj:
            session.delete(obj)
            session.commit()
            return True
        return False
```

---

## 5. 关键设计决策

- **JSONB 存规格和SKU**：自行车灯的规格字段因品牌而异（有的标流明，有的标照射距离），JSONB 灵活适配
- **platform + platform_id 做唯一约束**：替代当前的 `jd_product_id UNIQUE`，为多平台扩展做好准备
- **所有时间字段用 UTC**，展示层转换时区
- **价格用 DECIMAL(10,2)**：避免浮点精度问题

---

## 6. 实施步骤

| 步骤 | 内容 | 产出 |
|:---|:---|:---|
| 1 | 创建 `models/base.py` — engine + session工厂 | 数据库连接就绪 |
| 2 | 创建 `models/product.py` — Product, ProductImage, ProductSKU | 商品模型 |
| 3 | 创建 `models/review.py` — Review, ReviewImage | 评价模型 |
| 4 | 创建其余 model 文件 | 完整 ORM 模型 |
| 5 | 创建 `models/__init__.py` — 导出所有模型 + `init_db()` | 一行导入即用 |
| 6 | 创建 `models/crud.py` — 通用 CRUD 辅助 | 减少重复代码 |
| 7 | 更新 `db/init.sql` — 与 ORM 模型保持一致 | 文档同步 |
| 8 | 编写 `scripts/seed_test_data.py` — 插入 5-10 条假数据 | 开发测试用 |
